spring:
    datasource:
        url: jdbc:db2://desenvol1.uem.br:50000/desenvol
        username: db2bnd
        password: ibmdb2
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                dialect: org.hibernate.dialect.DB2Dialect
                format_sql: true
                id:
                    new_generator_mappings: false    
    tomcat:
        max-wait: 20000
        max-active: 50
        max-idle: 20
        min-idle: 15
    mvc:
        pathmatch:
            matching-strategy: ant_path_matcher      
          
server:
    port: 8080
    servlet:
        context-path: /
    forward-headers-strategy: framework

security:
    jwt:
        token:
            secret-key: jwt.PwD|0=>[npd]'{µ€ʍ}/22!
            expire-length: 300000 # 5 minutos (5 minutos * 60 segundos * 1000 milisegundos)
            
#springfox:
#    documentation:
#        enabled: true

springdoc:
    swagger-ui:
        enabled: true
        path: /swagger-ui.html
        defaultModelsExpandDepth: -1
        